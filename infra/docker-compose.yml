x-logging: &default-logging
  driver: json-file
  options:
    max-size: ${DOCKER_LOG_MAX_SIZE:-50m}
    max-file: ${DOCKER_LOG_MAX_FILES:-5}

services:
  postgres:
    image: postgres:16-alpine
    container_name: vms-postgres
    restart: unless-stopped
    logging: *default-logging
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-vms}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-change_me_postgres_password}
      POSTGRES_DB: ${POSTGRES_DB:-vms_db}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U \"$${POSTGRES_USER}\" -d \"$${POSTGRES_DB}\""]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 20s
    networks:
      - vms-net

  redis:
    image: redis:7-alpine
    container_name: vms-redis
    restart: unless-stopped
    logging: *default-logging
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - vms-net

  postgres-backup:
    image: postgres:16-alpine
    container_name: vms-postgres-backup
    restart: unless-stopped
    logging: *default-logging
    depends_on:
      - postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-vms}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-change_me_postgres_password}
      POSTGRES_DB: ${POSTGRES_DB:-vms_db}
      POSTGRES_BACKUP_RETENTION_DAYS: ${POSTGRES_BACKUP_RETENTION_DAYS:-14}
      POSTGRES_BACKUP_INTERVAL_SECONDS: ${POSTGRES_BACKUP_INTERVAL_SECONDS:-86400}
      # Bancos ALÉM do da aplicação. `drac_central` guarda o painel mestre:
      # instalações, licenças e credenciais de storage. Ficava de fora — o
      # pg_dump copiava só POSTGRES_DB, e o backup de arquivo da Central copiava
      # um JSON que virou legado somente-leitura quando o painel foi para modo
      # dual. O dado VIVO da Central não tinha cópia nenhuma, e os dois backups
      # pareciam estar rodando.
      # Bancos ALÉM do da aplicação. Vazio por padrão: `drac_central` só existe
      # no servidor MESTRE, e mantê-lo como default fazia toda instalação de
      # cliente registrar "postgres_backup=FALHOU db=drac_central" a cada
      # ciclo — um erro FALSO, perpétuo, que ensina o operador a ignorar falha
      # de backup. É o pior efeito colateral possível num alerta de backup.
      # O mestre declara explicitamente no seu infra/.env.
      POSTGRES_EXTRA_DATABASES: ${POSTGRES_EXTRA_DATABASES:-}
    volumes:
      - ./backups/postgres:/backups
    command: >
      sh -c 'set -eu;
      mkdir -p /backups;
      while true; do
        ts=$$(date -u +%Y%m%dT%H%M%SZ);
        export PGPASSWORD="$$POSTGRES_PASSWORD";
        tmp="/backups/.drac-postgres-$$ts.dump.tmp";
        out="/backups/drac-postgres-$$ts.dump";
        rm -f "$$tmp";
        pg_dump -h postgres -U "$$POSTGRES_USER" -d "$$POSTGRES_DB" -Fc -f "$$tmp";
        pg_restore --list "$$tmp" >/dev/null;
        mv "$$tmp" "$$out";
        echo "$$(date -u +%FT%TZ) postgres_backup=ok file=$$(basename "$$out")";
        for extra in $$POSTGRES_EXTRA_DATABASES; do
          etmp="/backups/.drac-$$extra-$$ts.dump.tmp";
          eout="/backups/drac-$$extra-$$ts.dump";
          rm -f "$$etmp";
          if pg_dump -h postgres -U "$$POSTGRES_USER" -d "$$extra" -Fc -f "$$etmp" 2>/dev/null; then
            pg_restore --list "$$etmp" >/dev/null;
            mv "$$etmp" "$$eout";
            echo "$$(date -u +%FT%TZ) postgres_backup=ok db=$$extra file=$$(basename "$$eout")";
          else
            rm -f "$$etmp";
            echo "$$(date -u +%FT%TZ) postgres_backup=FALHOU db=$$extra";
          fi;
        done;
        find /backups -type f -name "drac-*.dump" -mtime +$${POSTGRES_BACKUP_RETENTION_DAYS:-14} -delete;
        sleep "$${POSTGRES_BACKUP_INTERVAL_SECONDS:-86400}";
      done'
    networks:
      - vms-net

  postgres-backup-verify:
    image: postgres:16-alpine
    container_name: vms-postgres-backup-verify
    restart: unless-stopped
    logging: *default-logging
    depends_on:
      postgres:
        condition: service_healthy
      postgres-backup:
        condition: service_started
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-vms}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-change_me_postgres_password}
      POSTGRES_BACKUP_VERIFY_INTERVAL_SECONDS: ${POSTGRES_BACKUP_VERIFY_INTERVAL_SECONDS:-86400}
      POSTGRES_BACKUP_VERIFY_INITIAL_DELAY_SECONDS: ${POSTGRES_BACKUP_VERIFY_INITIAL_DELAY_SECONDS:-900}
    volumes:
      - ./backups/postgres:/backups:ro
      - ./verify-postgres-backup.sh:/verify-postgres-backup.sh:ro
    command: ["/bin/sh", "/verify-postgres-backup.sh"]
    networks:
      - vms-net

  central-data-backup:
    # Acompanha a Central: só existe onde ela existe.
    profiles: ["central"]
    image: alpine:3.20
    container_name: vms-central-data-backup
    restart: unless-stopped
    logging: *default-logging
    environment:
      CENTRAL_BACKUP_INTERVAL_SECONDS: ${CENTRAL_BACKUP_INTERVAL_SECONDS:-86400}
      CENTRAL_BACKUP_RETENTION_DAYS: ${CENTRAL_BACKUP_RETENTION_DAYS:-30}
    volumes:
      - ${CENTRAL_DATA_DIR:-../apps/central/data}:/central-data:ro
      - ./backups/central:/backups
      - ./backup-central-data.sh:/backup-central-data.sh:ro
    command: ["/bin/sh", "/backup-central-data.sh"]

  drac-central:
    # ── PAINEL MESTRE — NÃO roda no servidor do cliente ────────────────────
    #
    # A Central administra a FROTA de instalações; uma instalação de cliente é
    # administrada POR ela, não roda uma cópia dela. Sem este perfil, todo
    # `docker compose up` num servidor de cliente subia o painel mestre —
    # descoberto na instalação do D-GUARDIAN (07/08/2026), onde ele ainda
    # entrava em laço de reinício por não conseguir escrever seu arquivo de
    # dados. Além do desperdício, é confuso e desnecessário expor o painel da
    # operação inteira na máquina de um cliente.
    #
    # No servidor MESTRE, subir com `--profile central`.
    profiles: ["central"]
    build:
      context: ..
      dockerfile: apps/central/Dockerfile
    container_name: vms-central
    hostname: vms-central
    restart: unless-stopped
    init: true
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    logging: *default-logging
    ports:
      - "127.0.0.1:9765:9765"
    environment:
      - NODE_ENV=production
      - DRAC_CENTRAL_HOST=0.0.0.0
      - DRAC_CENTRAL_PORT=9765
      - DRAC_CENTRAL_DATA_FILE=/app/data/installations.json
      - DRAC_CENTRAL_BACKUP_DIR=/app/data/backups
      # BANCO DA CENTRAL, nunca o da aplicação. Esta linha FIXAVA
      # `@postgres:5432/vms_db` — o banco do VMS. O painel mestre passaria a
      # gravar as instalações dentro da base das câmeras, em modo dual, sem
      # nenhum aviso. O próprio datastore documenta esse risco e exige a URL
      # explícita justamente para não trocar de banco sozinho.
      #
      # Vazio = só JSON, que é o padrão seguro: melhor a Central não usar
      # Postgres do que usar o Postgres errado.
      - DRAC_CENTRAL_DATABASE_URL=${DRAC_CENTRAL_DATABASE_URL:-}
      - DRAC_CENTRAL_STORE_MODE=${DRAC_CENTRAL_STORE_MODE:-dual}
      - DRAC_CENTRAL_ADMIN_EMAIL=${DRAC_CENTRAL_ADMIN_EMAIL:-admin@drac.local}
      - DRAC_CENTRAL_ADMIN_PASSWORD_HASH=${DRAC_CENTRAL_ADMIN_PASSWORD_HASH:-}
      # Chave que cifra a credencial S3 de cada instalação. Estava AUSENTE aqui
      # e presente só no container que roda hoje: se este serviço subisse, a
      # credencial guardada viraria texto ilegível — a instalação pararia de
      # receber configuração de storage e a tela devolveria "não pôde ser
      # decifrada". Faltar esta linha era um defeito latente, não um detalhe.
      - CENTRAL_STORAGE_SECRET=${CENTRAL_STORAGE_SECRET:-}
      # Conta remetente compartilhada pela Central. Cada instalação escolhe
      # apenas os destinatários. Sem uma configuração completa, a própria tela
      # mantém o canal de e-mail indisponível — nunca aparenta estar ativo.
      - CENTRAL_SMTP_HOST=${CENTRAL_SMTP_HOST:-}
      - CENTRAL_SMTP_PORT=${CENTRAL_SMTP_PORT:-587}
      - CENTRAL_SMTP_SECURE=${CENTRAL_SMTP_SECURE:-false}
      - CENTRAL_SMTP_USER=${CENTRAL_SMTP_USER:-}
      - CENTRAL_SMTP_PASS=${CENTRAL_SMTP_PASS:-}
      - CENTRAL_SMTP_FROM=${CENTRAL_SMTP_FROM:-}
      - DRAC_CENTRAL_ADMIN_TOKEN=${DRAC_CENTRAL_ADMIN_TOKEN:-}
      - DRAC_CENTRAL_SESSION_HOURS=${DRAC_CENTRAL_SESSION_HOURS:-8}
      - DRAC_CENTRAL_ALLOWED_ORIGINS=${DRAC_CENTRAL_ALLOWED_ORIGINS:-https://localhost}
      - DRAC_CENTRAL_TRUSTED_PROXIES=${DRAC_CENTRAL_TRUSTED_PROXIES:-172.16.0.0/12}
      - DRAC_CENTRAL_COOKIE_SECURE=${DRAC_CENTRAL_COOKIE_SECURE:-true}
      - DRAC_CENTRAL_PUBLIC_URL=${DRAC_CENTRAL_PUBLIC_URL:-https://localhost/central}
      - DRAC_CENTRAL_INSTALLER_COMMIT=${DRAC_CENTRAL_INSTALLER_COMMIT:-}
      - DRAC_CENTRAL_INSTALLER_SHA256=${DRAC_CENTRAL_INSTALLER_SHA256:-}
      - DRAC_CENTRAL_INSTALLER_URL_TEMPLATE=${DRAC_CENTRAL_INSTALLER_URL_TEMPLATE:-https://raw.githubusercontent.com/TavaresEnok/DRAC/{commit}/scripts/install-drac.sh}
      - DRAC_CENTRAL_REPOSITORY_URL=${DRAC_CENTRAL_REPOSITORY_URL:-https://github.com/TavaresEnok/DRAC.git}
      - DRAC_CENTRAL_INSTALLER_TOKEN_TTL_SECONDS=${DRAC_CENTRAL_INSTALLER_TOKEN_TTL_SECONDS:-900}
      - DRAC_CENTRAL_INSTALLER_TOKEN_MAX_DOWNLOADS=${DRAC_CENTRAL_INSTALLER_TOKEN_MAX_DOWNLOADS:-3}
      - APP_BUILDER_AGENT_URL=${APP_BUILDER_AGENT_URL:-http://host.docker.internal:8780}
      - APP_BUILDER_AGENT_TOKEN=${APP_BUILDER_AGENT_TOKEN:-}
      - APP_BUILDER_AGENT_TIMEOUT_MS=${APP_BUILDER_AGENT_TIMEOUT_MS:-15000}
      # PORTA 8080, NÃO 80. O nginx do container `web` roda como usuário sem
      # privilégio e por isso escuta em 8080 (apps/web/nginx.conf: `listen 8080`).
      # Sem a porta, a Central batia na 80, levava ECONNREFUSED e devolvia
      # "Servidor de arquivos temporariamente indisponível" — mensagem que sugere
      # falha passageira quando na verdade nunca ia funcionar. As duas coisas
      # foram escritas no MESMO commit (79f68ba, 28/07) e nasceram divergentes:
      # o download de APK/AAB pela Central esteve quebrado desde então.
      - APK_SOURCE_BASE=http://web:8080
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ${CENTRAL_DATA_DIR:-../apps/central/data}:/app/data
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=64m
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:9765/api/health').then((r)=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - vms-net

  offsite-backup:
    image: rclone/rclone:1
    container_name: vms-offsite-backup
    profiles:
      - offsite-backup
    restart: unless-stopped
    logging: *default-logging
    environment:
      OFFSITE_BACKUP_REMOTE: ${OFFSITE_BACKUP_REMOTE:-}
      OFFSITE_BACKUP_INTERVAL_SECONDS: ${OFFSITE_BACKUP_INTERVAL_SECONDS:-86400}
      OFFSITE_INCLUDE_RECORDINGS: ${OFFSITE_INCLUDE_RECORDINGS:-false}
      OFFSITE_BACKUP_TRANSFERS: ${OFFSITE_BACKUP_TRANSFERS:-4}
      OFFSITE_BACKUP_CHECKERS: ${OFFSITE_BACKUP_CHECKERS:-8}
    volumes:
      - ./backups:/data/backups:ro
      - ./storage:/data/storage:ro
      - ${KEYSTORE_BACKUP_DIR:-/home/flashnet/keystore-backups}:/data/keystores:ro
      - ${RCLONE_CONFIG_PATH:-./rclone.conf}:/config/rclone/rclone.conf:ro
      - ./offsite-backup.sh:/offsite-backup.sh:ro
    entrypoint: ["/bin/sh", "/offsite-backup.sh"]

  # ── TRADUTOR DE DIALETO RTMP (SRS) ────────────────────────────────────────
  #
  # Atende a porta pública 1935 e repassa ao MediaMTX interno. Existe porque
  # câmera legada Adobe/FMLE (medido: Positivo CIP-B1312-M) exige a resposta
  # `onFCPublish` antes de enviar vídeo, e o MediaMTX 1.18.2 não a envia — a
  # câmera conecta, é aceita e desliga em ~150 ms sem um quadro. O SRS responde
  # e repassa; provado em laboratório com a câmera de teste ficando ONLINE.
  #
  # NÃO é fronteira de segurança: a autorização de publicação continua no
  # MediaMTX → mediamtx-auth do DRAC. Path não vinculado a uma câmera é recusado
  # lá e vira tentativa pendente, igual a antes. Aqui é só tradução de protocolo.
  #
  # Imagem fixada por digest; API/HTTP/DVR do SRS desligados na srs.conf.
  rtmp-ingest:
    image: ossrs/srs@sha256:4e293846ad2448ff1a0157aa2c694e7c451fff5046c93b5bc6da0fa0384ef998
    container_name: vms-rtmp-ingest
    restart: unless-stopped
    # Endurecido em paridade com api/mediamtx: este é um parser RTMP em C++
    # exposto direto à internet na 1935, então minimiza-se o que um bug de
    # memória no handshake poderia alcançar.
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    pids_limit: 256
    mem_limit: 256m
    # O SRS escreve só o pid; read_only + este tmpfs cobre isso.
    tmpfs:
      - /tmp:rw,noexec,nosuid,size=8m
    logging: *default-logging
    command: ["./objs/srs", "-c", "conf/srs.conf"]
    ports:
      - "${MEDIAMTX_RTMP_PORT:-1935}:1935/tcp"
    volumes:
      - ./rtmp-ingest/srs.conf:/usr/local/srs/conf/srs.conf:ro
    depends_on:
      - mediamtx
    # SÓ rtmp-net: o tradutor alcança o MediaMTX e nada mais. Sem acesso a
    # postgres/redis/api, um SRS comprometido não tem para onde se mover.
    networks:
      - rtmp-net

  adminer:
    image: adminer:latest
    container_name: vms-adminer
    profiles:
      - admin-tools
    restart: unless-stopped
    logging: *default-logging
    networks:
      - vms-net

  api:
    build:
      context: ..
      dockerfile: apps/api/Dockerfile
    container_name: vms-api
    restart: unless-stopped
    init: true
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    logging: *default-logging
    # Porta em LOOPBACK: o nginx (host) faz proxy de /api/ p/ 127.0.0.1:3000. Fica no
    # compose BASE (não só no dev override) — recriar sem o override dropava a porta e
    # derrubava /api inteiro (502). Só o nginx alcança; o público não bate direto.
    ports:
      # Mesma variável do overlay de produção — entradas idênticas deduplicam.
      # Fixar 127.0.0.1 aqui + ${DRAC_API_BIND}=0.0.0.0 no overlay criava DUAS
      # ligações da mesma porta e a API nunca subia (mesmo defeito do MediaMTX,
      # commit 43aa314). O padrão continua loopback.
      - "${DRAC_API_BIND:-127.0.0.1}:3000:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgresql://${POSTGRES_USER:-vms}:${POSTGRES_PASSWORD:-change_me_postgres_password}@postgres:5432/${POSTGRES_DB:-vms_db}
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_CONNECT_TIMEOUT_MS=${REDIS_CONNECT_TIMEOUT_MS:-5000}
      - REDIS_STARTUP_TIMEOUT_MS=${REDIS_STARTUP_TIMEOUT_MS:-10000}
      - API_PORT=3000
      - CORS_ALLOWED_ORIGINS=${CORS_ALLOWED_ORIGINS:-http://localhost:5173,http://127.0.0.1:5173}
      - PUBLIC_APP_URL=${PUBLIC_APP_URL:-}
      - API_PUBLIC_URL=${API_PUBLIC_URL:-}
      - COOKIE_SECURE=${COOKIE_SECURE:-}
      - TRUST_PROXY_HOPS=${TRUST_PROXY_HOPS:-0}
      - JWT_SECRET=${JWT_SECRET:?JWT_SECRET is required}
      - AUTH_REFRESH_INACTIVITY_DAYS=${AUTH_REFRESH_INACTIVITY_DAYS:-7}
      - CAMERA_SECRET_KEY=${CAMERA_SECRET_KEY:?CAMERA_SECRET_KEY is required}
      # ENVIO PARA A NUVEM. O padrão do código (25 por rodada) não sustentava
      # nem o regime permanente desta frota: MEDIDO, 26 câmeras em movimento
      # produzem ~312 gravações/hora, e 25 a cada 15 min entrega 100/hora — a
      # fila só crescia. Com 100 a cada 15 min são 400/hora, ~28% de folga.
      #
      # Essa folga é apertada de propósito para não competir com o vídeo ao vivo,
      # mas tem consequência: uma parada de 2h cria ~620 de fila, que leva ~7h
      # para drenar. Se a frota crescer, este número sobe junto.
      - CLOUD_OFFLOAD_BATCH=${CLOUD_OFFLOAD_BATCH:-100}
      - CLOUD_OFFLOAD_INTERVAL_MINUTES=${CLOUD_OFFLOAD_INTERVAL_MINUTES:-15}
      - CAMERA_SECRET_KEY_LEGACY=${CAMERA_SECRET_KEY_LEGACY:-}
      - INTERNAL_SERVICE_TOKEN=${INTERNAL_SERVICE_TOKEN:?INTERNAL_SERVICE_TOKEN is required}
      - EVIDENCE_HMAC_SECRET=${EVIDENCE_HMAC_SECRET:?EVIDENCE_HMAC_SECRET is required}
      - EVIDENCE_HMAC_KEY_ID=${EVIDENCE_HMAC_KEY_ID:-local-v1}
      - CLOUD_CONNECTOR_ENABLED=${CLOUD_CONNECTOR_ENABLED:-false}
      - CLOUD_API_URL=${CLOUD_API_URL:-}
      - CLOUD_INSTALLATION_ID=${CLOUD_INSTALLATION_ID:-}
      - CLOUD_LICENSE_KEY=${CLOUD_LICENSE_KEY:-}
      - CLOUD_CUSTOMER_NAME=${CLOUD_CUSTOMER_NAME:-}
      - CLOUD_HEARTBEAT_INTERVAL_SECONDS=${CLOUD_HEARTBEAT_INTERVAL_SECONDS:-60}
      - CLOUD_CONNECTOR_TIMEOUT_MS=${CLOUD_CONNECTOR_TIMEOUT_MS:-8000}
      - DRAC_VERSION=${DRAC_VERSION:-local}
      - DRAC_LAUNCH_PROFILE=${DRAC_LAUNCH_PROFILE:-standard}
      - RECORDINGS_ROOT=/storage
      # QUEM GRAVA. `local` (default) = a API sobe o ffmpeg; `worker` = a API só
      # publica o comando e quem grava é o camera-worker (profile legacy-worker).
      # Precisa ser o MESMO valor nos dois serviços: com a API em `local` e o
      # worker no ar, os dois gravariam as mesmas câmeras em paralelo. O worker
      # recusa subir fora do modo `worker` justamente para tornar isso impossível.
      - RECORDING_CONTROL_MODE=${RECORDING_CONTROL_MODE:-local}
      - POSTGRES_BACKUP_DIR=/backups/postgres
      - RECORDING_AUTO_START_ENABLED=${RECORDING_AUTO_START_ENABLED:-false}
      - RECORDING_AUTO_START_DELAY_MS=${RECORDING_AUTO_START_DELAY_MS:-10000}
      - RECORDING_DISK_GUARD_ENABLED=${RECORDING_DISK_GUARD_ENABLED:-true}
      - RECORDING_DISK_GUARD_INTERVAL_MS=${RECORDING_DISK_GUARD_INTERVAL_MS:-30000}
      - RECORDING_DISK_GUARD_MAX_USED_PERCENT=${RECORDING_DISK_GUARD_MAX_USED_PERCENT:-92}
      - MOTION_RECORDING_POST_ROLL_SECONDS=${MOTION_RECORDING_POST_ROLL_SECONDS:-90}
      - MOTION_RECORDING_SEGMENT_SECONDS=${MOTION_RECORDING_SEGMENT_SECONDS:-120}
      - MOTION_ALARM_QUIET_SECONDS=${MOTION_ALARM_QUIET_SECONDS:-300}
      # IA de identificação de OBJETO/FACE desativada (ainda não pronta p/ uso). O
      # filtro semântico ONVIF invoca o detector YOLO p/ confirmar objeto antes de
      # gravar movimento nativo; com false, o movimento nativo grava direto (fail-safe),
      # SEM carregar/rodar o detector de objeto. A análise de stream já é só o MOG2
      # (AiSettings.mode='motion'). Reative com 'true' quando a IA de objeto estiver pronta.
      - AI_ONVIF_SEMANTIC_FILTER=${AI_ONVIF_SEMANTIC_FILTER:-false}
      # Source Gateway: consumidores que só precisam de QUADROS (IA, grade, poster)
      # leem da origem já republicada pelo MediaMTX em vez de abrir mais uma
      # conexão na câmera. Gravação/pré-buffer/clipe NUNCA são reroteados (o
      # `-c copy` do bitstream original é invariante de qualidade).
      - CAMERA_SOURCE_GATEWAY_ENABLED=${CAMERA_SOURCE_GATEWAY_ENABLED:-true}
      - FFMPEG_RECORDING_CODEC_MODE=${FFMPEG_RECORDING_CODEC_MODE:-copy}
      - RETENTION_DAYS=${RETENTION_DAYS:-7}
      - RECORDING_RETENTION_DAYS=${RECORDING_RETENTION_DAYS:-7}
      - RETENTION_USE_BULLMQ=${RETENTION_USE_BULLMQ:-true}
      - MEDIAMTX_ENABLED=true
      - MEDIAMTX_API_BASE_URL=${MEDIAMTX_API_BASE_URL:-http://mediamtx:9997}
      - MEDIAMTX_RTSP_INTERNAL_URL=${MEDIAMTX_RTSP_INTERNAL_URL:-rtsp://mediamtx:8554}
      - MEDIAMTX_API_USER=${MEDIAMTX_API_USER:?MEDIAMTX_API_USER is required}
      - MEDIAMTX_API_PASS=${MEDIAMTX_API_PASS:?MEDIAMTX_API_PASS is required}
      - MEDIAMTX_AUTH_CALLBACK_TOKEN=${MEDIAMTX_AUTH_CALLBACK_TOKEN:?MEDIAMTX_AUTH_CALLBACK_TOKEN is required}
      - MEDIAMTX_PUBLIC_HOST=${MEDIAMTX_PUBLIC_HOST:-}
      # Host/IP curto usado somente em URLs RTMP de campo único. Intelbras
      # limita "Endereço personalizado" a 63 caracteres; nunca encurte a chave.
      - MEDIAMTX_RTMP_SHORT_HOST=${MEDIAMTX_RTMP_SHORT_HOST:-}
      - MEDIAMTX_PUBLIC_SCHEME=${MEDIAMTX_PUBLIC_SCHEME:-}
      - MEDIAMTX_PUBLIC_WEBRTC_URL=${MEDIAMTX_PUBLIC_WEBRTC_URL:-}
      - MEDIAMTX_PUBLIC_HLS_URL=${MEDIAMTX_PUBLIC_HLS_URL:-}
      - MEDIAMTX_HLS_PORT=${MEDIAMTX_HLS_PORT:-8888}
      - MEDIAMTX_WEBRTC_PORT=${MEDIAMTX_WEBRTC_PORT:-8889}
      - MEDIAMTX_HLS_ALLOW_ORIGIN=${MEDIAMTX_HLS_ALLOW_ORIGIN:-*}
      - MEDIAMTX_WEBRTC_ALLOW_ORIGIN=${MEDIAMTX_WEBRTC_ALLOW_ORIGIN:-*}
      - MEDIAMTX_WEBRTC_ADDITIONAL_HOST=${MEDIAMTX_WEBRTC_ADDITIONAL_HOST:-127.0.0.1}
      - MEDIAMTX_SOURCE_ON_DEMAND=${MEDIAMTX_SOURCE_ON_DEMAND:-false}
      - MEDIAMTX_SOURCE_ON_DEMAND_START_TIMEOUT=${MEDIAMTX_SOURCE_ON_DEMAND_START_TIMEOUT:-6s}
      - MEDIAMTX_SOURCE_ON_DEMAND_CLOSE_AFTER=${MEDIAMTX_SOURCE_ON_DEMAND_CLOSE_AFTER:-5m}
      - MEDIAMTX_RUN_ON_DEMAND_CLOSE_AFTER=${MEDIAMTX_RUN_ON_DEMAND_CLOSE_AFTER:-5m}
      - MEDIAMTX_SELECTED_RUN_ON_DEMAND_CLOSE_AFTER=${MEDIAMTX_SELECTED_RUN_ON_DEMAND_CLOSE_AFTER:-90s}
      - MEDIAMTX_WARM_PATHS_ON_BOOT=${MEDIAMTX_WARM_PATHS_ON_BOOT:-true}
      - MEDIAMTX_WARM_SELECTED_PATHS_ON_BOOT=${MEDIAMTX_WARM_SELECTED_PATHS_ON_BOOT:-false}
      - ALARM_WEBHOOK_ALLOWED_HOSTS=${ALARM_WEBHOOK_ALLOWED_HOSTS:-}
      - ALARM_WEBHOOK_SIGNING_SECRET=${ALARM_WEBHOOK_SIGNING_SECRET:-}
      - CAMERA_TEST_ALLOW_PUBLIC_IP=${CAMERA_TEST_ALLOW_PUBLIC_IP:-false}
      - CAMERA_ALLOWED_CIDRS=${CAMERA_ALLOWED_CIDRS:-}
      - CAMERA_DENIED_CIDRS=${CAMERA_DENIED_CIDRS:-}
      # Quantas câmeras degradadas o health check re-sonda por ciclo (60s).
      # NÃO é um número livre: cada sonda abre uma sessão RTSP no equipamento do
      # cliente, e DVR tem limite de sessões concorrentes. Alto demais e a sonda
      # come as sessões que a IA/gravação precisam — o DVR passa a responder
      # "SETUP failed: 503 ServerUnavailable" e a gravação por movimento morre
      # enquanto o painel jura que está só "verificando saúde".
      - HEALTH_AUTO_REMEDIATION_MAX_PER_RUN=${HEALTH_AUTO_REMEDIATION_MAX_PER_RUN:-5}
      # Sonda RTSP: stream principal Hikvision via WAN precisa de >5,5s para o
      # ffprobe fechar metadados; matar cedo = câmera saudável marcada OFFLINE.
      - CAMERA_RTSP_PROBE_TIMEOUT_MS=${CAMERA_RTSP_PROBE_TIMEOUT_MS:-4500}
      - CAMERA_RTSP_PROBE_KILL_TIMEOUT_MS=${CAMERA_RTSP_PROBE_KILL_TIMEOUT_MS:-5500}
      # Fontes quentes da grade: por RELEVÂNCIA (últimas vistas), com teto.
      # 64 cobre qualquer instalação pequena inteira; numa frota de 2.000 só as
      # 64 câmeras realmente olhadas ficam conectadas — o resto liga sob
      # demanda (~5s no primeiro acesso e entra no conjunto quente na hora).
      - MEDIAMTX_HOT_GRID_SOURCES_MAX=${MEDIAMTX_HOT_GRID_SOURCES_MAX:-64}
      - MEDIAMTX_HOT_GRID_WINDOW_HOURS=${MEDIAMTX_HOT_GRID_WINDOW_HOURS:-168}
      - FFMPEG_RTSP_FALLBACK_TRANSPORTS=tcp,udp
      - FFMPEG_RTSP_FALLBACK_PORTS=51488,51489,51490
      - FFMPEG_RTSP_ENABLE_PORT_FALLBACK=false
      - AI_AUTO_START_ENABLED=${AI_AUTO_START_ENABLED:-true}
      - AI_USE_MEDIAMTX=${AI_USE_MEDIAMTX:-false}
      - AI_RTSP_SUBTYPE=${AI_RTSP_SUBTYPE:-auto}
      - AI_ENABLED_CAMERA_IDS=${AI_ENABLED_CAMERA_IDS:-}
      - AI_ACTIVE_CAMERA_IDS=${AI_ACTIVE_CAMERA_IDS:-}
      - AI_FORCE_SINGLE_CAMERA=${AI_FORCE_SINGLE_CAMERA:-false}
      - AI_SINGLE_CAMERA_ID=${AI_SINGLE_CAMERA_ID:-}
      - AI_ANALYTICS_CAMERA_IDS=${AI_ANALYTICS_CAMERA_IDS:-}
      - AI_RTSP_STREAM_PROFILE=${AI_RTSP_STREAM_PROFILE:-analytics}
      - AI_ANALYTICS_SOURCE=${AI_ANALYTICS_SOURCE:-direct_camera}
      - AI_ANALYTICS_HEVC_FALLBACK=${AI_ANALYTICS_HEVC_FALLBACK:-true}
      # Agente de build white-label (roda no HOST; ver scripts/build-agent.mjs)
      - APP_BUILDER_AGENT_URL=${APP_BUILDER_AGENT_URL:-http://host.docker.internal:8780}
      - APP_BUILDER_AGENT_TOKEN=${APP_BUILDER_AGENT_TOKEN:-}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./storage:/storage
      - ./backups/postgres:/backups/postgres:ro
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=256m
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      mediamtx:
        condition: service_started
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:3000/health').then((r)=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 40s
    networks:
      - vms-net

  web:
    build:
      context: ..
      dockerfile: apps/web/Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-}
    container_name: vms-web
    restart: unless-stopped
    init: true
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    logging: *default-logging
    # Porta em LOOPBACK: o nginx (host) faz proxy de / p/ 127.0.0.1:5173 (o web serve
    # na 80 interna). No compose BASE pelo mesmo motivo da api/mediamtx (não depender do
    # override — recriar sem ele derrubava o site).
    ports:
      - "${DRAC_WEB_BIND:-127.0.0.1}:5173:8080"
    # APKs white-label publicados pelo build-agent ficam num diretório do HOST
    # montado aqui (somente leitura). Sobrevive a rebuilds do container — antes os
    # APKs eram copiados pra dentro do container e sumiam a cada recriação.
    volumes:
      - ./apk:/usr/share/nginx/html/apk:ro
    # `uid`/`gid` do usuário nginx (101) são OBRIGATÓRIOS aqui.
    #
    # O container roda como `nginx` (não-root) com `read_only: true`, e um tmpfs
    # monta VAZIO e pertencendo a root — mascarando os diretórios que a imagem
    # trazia. Sem o uid/gid, o nginx não consegue criar `client_temp` e o
    # container entra em laço de reinício com
    # "mkdir() /var/cache/nginx/client_temp failed (13: Permission denied)".
    tmpfs:
      - /var/cache/nginx:rw,noexec,nosuid,nodev,size=64m,uid=101,gid=101
      - /var/run:rw,noexec,nosuid,nodev,size=8m,uid=101,gid=101
    depends_on:
      - api
      # `drac-central` NÃO entra aqui: ele vive sob o perfil "central" e não
      # existe numa instalação de cliente. Declarar a dependência tornava o
      # arquivo INVÁLIDO sem o perfil ("depends on undefined service"), o que
      # derrubaria toda instalação nova. O nginx do web só encaminha /central/
      # quando há para onde encaminhar — a ausência é tratada em tempo de
      # requisição, não de composição.
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8080/ >/dev/null 2>&1 || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 20s
    networks:
      - vms-net

  camera-worker:
    build:
      context: ..
      dockerfile: services/camera-worker-go/Dockerfile
    container_name: vms-camera-worker
    profiles:
      - legacy-worker
    restart: unless-stopped
    init: true
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    logging: *default-logging
    environment:
      - REDIS_ADDR=redis:6379
      - API_URL=http://api:3000
      - CAMERA_SECRET_KEY=${CAMERA_SECRET_KEY:?CAMERA_SECRET_KEY is required}
      - INTERNAL_SERVICE_TOKEN=${INTERNAL_SERVICE_TOKEN:?INTERNAL_SERVICE_TOKEN is required}
      - STORAGE_ROOT=/storage
      # MESMO valor do serviço `api` (ver comentário lá). O worker RECUSA subir se
      # não for `worker` — sem isso, habilitar este profile com a API no default
      # `local` faria API e worker gravarem a mesma câmera ao mesmo tempo.
      - RECORDING_CONTROL_MODE=${RECORDING_CONTROL_MODE:-local}
    volumes:
      - ./storage:/storage
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=128m
    depends_on:
      - redis
      - api
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8000/health >/dev/null 2>&1 || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 40s
    networks:
      - vms-net

  ai-service:
    # Sobe junto com o stack (sem profile): a detecção de movimento é parte do
    # sistema. Fica ocioso (~0 CPU) até uma câmera ser armada para gravação por
    # movimento — só as armadas são analisadas (ver ai-manager filtro 'motion').
    build:
      context: ../services/ai-service-python
      dockerfile: Dockerfile
    container_name: vms-ai-service
    restart: unless-stopped
    init: true
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    logging: *default-logging
    expose:
      - "8000"
    environment:
      - REDIS_ADDR=redis:6379
      - API_URL=http://api:3000
      - INTERNAL_SERVICE_TOKEN=${INTERNAL_SERVICE_TOKEN:?INTERNAL_SERVICE_TOKEN is required}
      - AI_MODELS_DIR=/app/models
      - GENERAL_MODE=${GENERAL_MODE:-general}
      - GENERAL_MODEL=${GENERAL_MODEL:-yolo26n}
      - GENERAL_RUNTIME=${GENERAL_RUNTIME:-openvino_cpu}
      - GENERAL_PRECISION=${GENERAL_PRECISION:-int8}
      - GENERAL_ANALYSIS_WIDTH=${GENERAL_ANALYSIS_WIDTH:-960}
      - GENERAL_ANALYSIS_HEIGHT=${GENERAL_ANALYSIS_HEIGHT:-540}
      - GENERAL_IMGSZ=${GENERAL_IMGSZ:-640}
      - GENERAL_DETECTION_FPS=${GENERAL_DETECTION_FPS:-4.0}
      - GENERAL_MOTION_TRIGGER=${GENERAL_MOTION_TRIGGER:-SYSTEM}
      - GENERAL_EVENT_DEBOUNCE_SECONDS=${GENERAL_EVENT_DEBOUNCE_SECONDS:-10}
      - MOTION_EVENT_DEBOUNCE_SECONDS=${MOTION_EVENT_DEBOUNCE_SECONDS:-45}
      # ⚠️ TERCEIRA via de IA de objeto, fácil de perder de vista: mesmo com
      # `analysis_type=motion` e `advanced_analysis_type=None`, o stream_processor
      # roda um detector de objeto sobre o recorte do movimento para RÓTULAR o
      # evento (moto, ônibus, pessoa…). É o `MOTION_SEMANTIC_CONFIRM`, cujo
      # DEFAULT NO CÓDIGO É `true` — desligar AI_ONVIF_SEMANTIC_FILTER (caminho
      # ONVIF) e checar advanced_analysis_type NÃO basta: sem esta linha a
      # identificação de objeto continua acontecendo no caminho de movimento.
      - MOTION_SEMANTIC_CONFIRM=${MOTION_SEMANTIC_CONFIRM:-false}
      - GENERAL_CLASSES=${GENERAL_CLASSES:-person,bicycle,car,motorcycle,bus}
      - GENERAL_CLASS_IDS=${GENERAL_CLASS_IDS:-0,1,2,3,5}
      - GENERAL_CONFIDENCE_PERSON=${GENERAL_CONFIDENCE_PERSON:-0.30}
      - GENERAL_CONFIDENCE_BICYCLE=${GENERAL_CONFIDENCE_BICYCLE:-0.25}
      - GENERAL_CONFIDENCE_CAR=${GENERAL_CONFIDENCE_CAR:-0.25}
      - GENERAL_CONFIDENCE_MOTORCYCLE=${GENERAL_CONFIDENCE_MOTORCYCLE:-0.25}
      - GENERAL_CONFIDENCE_VEHICLE=${GENERAL_CONFIDENCE_VEHICLE:-0.25}
      - GENERAL_MIN_OBJECT_HEIGHT_PX=${GENERAL_MIN_OBJECT_HEIGHT_PX:-10}
      - GENERAL_DETECT_VEHICLES=${GENERAL_DETECT_VEHICLES:-false}
      - GENERAL_DETECT_OBJECTS=${GENERAL_DETECT_OBJECTS:-false}
      - GENERAL_DETECT_ANIMALS=${GENERAL_DETECT_ANIMALS:-false}
      - GENERAL_RECOGNITION=${GENERAL_RECOGNITION:-false}
      - GENERAL_FACE_DETECTION=${GENERAL_FACE_DETECTION:-false}
      - GENERAL_TRACKER=${GENERAL_TRACKER:-bytetrack}
      # Pacote de tracking (15/08/2026). Defaults SEGUROS: rider e compensação
      # de câmera desligados; o tracker novo só entra por escolha explícita.
      - GENERAL_TRACKER_LOW_CONF_FLOOR=${GENERAL_TRACKER_LOW_CONF_FLOOR:-0.10}
      - GENERAL_TRACKER_RECOVERY_GRACE_MS=${GENERAL_TRACKER_RECOVERY_GRACE_MS:-2000}
      - GENERAL_STATIONARY_FRAMES=${GENERAL_STATIONARY_FRAMES:-10}
      - GENERAL_STATIONARY_IOU=${GENERAL_STATIONARY_IOU:-0.88}
      - GENERAL_STATIONARY_OUT_IOU=${GENERAL_STATIONARY_OUT_IOU:-0.70}
      - GENERAL_STATIONARY_COAST=${GENERAL_STATIONARY_COAST:-true}
      - GENERAL_CAMERA_MOTION_COMP=${GENERAL_CAMERA_MOTION_COMP:-false}
      - GENERAL_TRACKER_APPEARANCE=${GENERAL_TRACKER_APPEARANCE:-true}
      - GENERAL_TRACKER_APPEARANCE_VETO=${GENERAL_TRACKER_APPEARANCE_VETO:-0.10}
      - GENERAL_TRACKER_MIN_HITS=${GENERAL_TRACKER_MIN_HITS:-1}
      - GENERAL_RIDER_ASSOCIATION=${GENERAL_RIDER_ASSOCIATION:-false}
      - GENERAL_RIDER_PERSON_FLOOR=${GENERAL_RIDER_PERSON_FLOOR:-0.12}
      - GENERAL_RIDER_VEHICLE_MIN=${GENERAL_RIDER_VEHICLE_MIN:-0.45}
      - GENERAL_RIDER_CONFIRM_FRAMES=${GENERAL_RIDER_CONFIRM_FRAMES:-2}
      - GENERAL_CPU_MODEL_CEILING=${GENERAL_CPU_MODEL_CEILING:-yolo26s}
      - GENERAL_TRACK_BUFFER=${GENERAL_TRACK_BUFFER:-20}
      - GENERAL_PERSISTENT_TRACK_ID=${GENERAL_PERSISTENT_TRACK_ID:-true}
      - GENERAL_SHOW_AFTER_HITS=${GENERAL_SHOW_AFTER_HITS:-1}
      - GENERAL_HIDE_AFTER_MISSES=${GENERAL_HIDE_AFTER_MISSES:-5}
      - GENERAL_LOST_TTL_MS=${GENERAL_LOST_TTL_MS:-2000}
      - GENERAL_OVERLAY_MODE=${GENERAL_OVERLAY_MODE:-box}
      - GENERAL_OVERLAY_TTL_MS=${GENERAL_OVERLAY_TTL_MS:-1800}
      - GENERAL_EMIT_EVENTS=${GENERAL_EMIT_EVENTS:-true}
      - GENERAL_MODEL_PATH=${GENERAL_MODEL_PATH:-}
      - GENERAL_MODEL_INPUT_WIDTH=${GENERAL_MODEL_INPUT_WIDTH:-0}
      - GENERAL_MODEL_INPUT_HEIGHT=${GENERAL_MODEL_INPUT_HEIGHT:-0}
      - GENERAL_MODEL_DYNAMIC=${GENERAL_MODEL_DYNAMIC:-false}
      - GENERAL_MODEL_END2END=${GENERAL_MODEL_END2END:-true}
      - GENERAL_MODEL_NMS=${GENERAL_MODEL_NMS:-false}
      - GENERAL_OPENVINO_DEVICE=${GENERAL_OPENVINO_DEVICE:-CPU}
      - GENERAL_OPENVINO_PERFORMANCE_HINT=${GENERAL_OPENVINO_PERFORMANCE_HINT:-LATENCY}
      - GENERAL_INFERENCE_THREADS=${GENERAL_INFERENCE_THREADS:-}
      - GENERAL_INFERENCE_WORKERS=${GENERAL_INFERENCE_WORKERS:-}
      - AI_ENABLED_CAMERA_IDS=${AI_ENABLED_CAMERA_IDS:-}
      - AI_ACTIVE_CAMERA_IDS=${AI_ACTIVE_CAMERA_IDS:-}
      - AI_FORCE_SINGLE_CAMERA=${AI_FORCE_SINGLE_CAMERA:-false}
      - AI_SINGLE_CAMERA_ID=${AI_SINGLE_CAMERA_ID:-}
      - AI_ANALYTICS_CAMERA_IDS=${AI_ANALYTICS_CAMERA_IDS:-}
      - AI_RTSP_STREAM_PROFILE=${AI_RTSP_STREAM_PROFILE:-analytics}
      - AI_ANALYTICS_SOURCE=${AI_ANALYTICS_SOURCE:-direct_camera}
      - AI_ANALYTICS_HEVC_FALLBACK=${AI_ANALYTICS_HEVC_FALLBACK:-true}
      - AI_LATEST_FRAME_ONLY=${AI_LATEST_FRAME_ONLY:-true}
      - AI_CPU_RESERVE_PERCENT=${AI_CPU_RESERVE_PERCENT:-25}
      - AI_INFERENCE_WORKER_COUNT=${AI_INFERENCE_WORKER_COUNT:-}
      # DEIXE VAZIO. O serviço mede os núcleos REAIS da máquina e calcula o
      # orçamento (descontando AI_CPU_RESERVE_PERCENT) — que é o que uma frota
      # de servidores diferentes precisa. Cravar um número quebra em toda
      # máquina menor que aquela onde ele foi escolhido.
      # MEDIDO em 13/08/2026 (6 núcleos, yolo26n INT8 640 OpenVINO, mediana de
      # 12 inferências): 2→24,7ms · 3→17,5 · 4→14,1 · 5→11,8 · 6→11,7 · 8→11,9 ·
      # 10→12,9. A curva satura em 5-6 e PIORA acima disso. A instalação vinha
      # com 10 cravado: +9% de latência e CPU roubada da gravação, com o próprio
      # serviço reportando `within_budget: false` no /health sem ninguém ler.
      - AI_INFERENCE_THREADS_OVERRIDE=${AI_INFERENCE_THREADS_OVERRIDE:-}
      - AI_SELECTED_DETECTION_FPS=${AI_SELECTED_DETECTION_FPS:-3.0}
      - AI_SELECTED_ANALYSIS_WIDTH=${AI_SELECTED_ANALYSIS_WIDTH:-960}
      - AI_SELECTED_ANALYSIS_HEIGHT=${AI_SELECTED_ANALYSIS_HEIGHT:-540}
      - AI_SELECTED_IMGSZ=${AI_SELECTED_IMGSZ:-640}
      - AI_GRID_DETECTION_FPS=${AI_GRID_DETECTION_FPS:-1.0}
      - AI_GRID_ANALYSIS_WIDTH=${AI_GRID_ANALYSIS_WIDTH:-640}
      - AI_GRID_ANALYSIS_HEIGHT=${AI_GRID_ANALYSIS_HEIGHT:-360}
      - AI_GRID_IMGSZ=${AI_GRID_IMGSZ:-512}
      - AI_QOS_LIVE_ENABLED=${AI_QOS_LIVE_ENABLED:-true}
      - AI_ADAPTIVE_MODE=${AI_ADAPTIVE_MODE:-true}
      - AI_ADAPTIVE_PILOT_CAMERA_IDS=${AI_ADAPTIVE_PILOT_CAMERA_IDS:-*}
      - AI_ADAPTIVE_WINDOW_SECONDS=${AI_ADAPTIVE_WINDOW_SECONDS:-6}
      - AI_ADAPTIVE_DEGRADE_COOLDOWN_SECONDS=${AI_ADAPTIVE_DEGRADE_COOLDOWN_SECONDS:-8}
      - AI_ADAPTIVE_RECOVER_COOLDOWN_SECONDS=${AI_ADAPTIVE_RECOVER_COOLDOWN_SECONDS:-15}
      - AI_ADAPTIVE_DROP_RATIO_HIGH=${AI_ADAPTIVE_DROP_RATIO_HIGH:-0.12}
      - AI_ADAPTIVE_DROP_RATIO_LOW=${AI_ADAPTIVE_DROP_RATIO_LOW:-0.03}
      - AI_ADAPTIVE_CPU_HIGH=${AI_ADAPTIVE_CPU_HIGH:-88}
      - AI_ADAPTIVE_CPU_LOW=${AI_ADAPTIVE_CPU_LOW:-68}
      - AI_ADAPTIVE_SELECTED_FPS_FACTORS=${AI_ADAPTIVE_SELECTED_FPS_FACTORS:-1.0,0.85,0.70}
      - AI_ADAPTIVE_GRID_FPS_FACTORS=${AI_ADAPTIVE_GRID_FPS_FACTORS:-1.0,0.85}
      - AI_ADAPTIVE_SELECTED_IMGSZ_STEPS=${AI_ADAPTIVE_SELECTED_IMGSZ_STEPS:-640,512,416}
      - AI_ADAPTIVE_GRID_IMGSZ_STEPS=${AI_ADAPTIVE_GRID_IMGSZ_STEPS:-512,416}
      - AI_ADAPTIVE_SELECTED_RESOLUTION_SCALES=${AI_ADAPTIVE_SELECTED_RESOLUTION_SCALES:-1.0,0.92,0.84,0.76}
      - AI_ADAPTIVE_GRID_RESOLUTION_SCALES=${AI_ADAPTIVE_GRID_RESOLUTION_SCALES:-1.0,0.90,0.80}
      - AI_READINESS_MAX_FRAME_AGE_SECONDS=${AI_READINESS_MAX_FRAME_AGE_SECONDS:-20}
      - PYTHONDONTWRITEBYTECODE=1
    volumes:
      - ./storage:/storage
      - ./ai-models:/app/models
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=512m
    depends_on:
      - redis
      - api
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8000/health >/dev/null 2>&1 || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 40s
    networks:
      - vms-net

  mediamtx:
    # Imagem FIXADA POR DIGEST (2026-07-21). A tag `1-ffmpeg` é móvel: qualquer
    # recriação futura poderia puxar outra minor do 1.x sem aviso, trocando o
    # servidor de mídia de produção por baixo (já tivemos incidentes de /live com
    # mudanças de configuração do MediaMTX). O digest abaixo é exatamente a
    # v1.18.2 validada em produção. Para atualizar: testar a nova versão e
    # trocar o digest conscientemente.
    image: bluenviron/mediamtx:1-ffmpeg@sha256:65a3d7fff1debd4a33846eb0f3c28326ecb871d1359b6ad951848ace91ea1b20
    container_name: vms-mediamtx
    restart: unless-stopped
    # PORTAS (não deixar só no docker-compose.dev.yml — recriar o container sem o override
    # dropava estas portas e derrubava o /live inteiro: nginx batia em porta morta → 502 →
    # câmeras eternas em "conectando..."). HLS e WebRTC-signaling ficam só em loopback; quem
    # expõe ao mundo é o nginx (HTTPS) via /hls/ e /webrtc/. RTSP (8554) e API (9997) seguem
    # internos à rede docker (api/ai-service/gravação alcançam por rtsp://mediamtx / :9997).
    ports:
      # ── AS MESMAS VARIÁVEIS DO OVERLAY DE PRODUÇÃO ──────────────────────
      #
      # Estas linhas precisam existir no BASE (recriar o container sem o
      # override dropava as portas e derrubava o /live inteiro), mas fixar
      # `127.0.0.1` aqui quebrava toda instalação de produção: o overlay
      # publica `${DRAC_MEDIAMTX_*_BIND}`, o Compose SOMA as listas em vez de
      # substituir, e um `.env` com 0.0.0.0 gerava DUAS ligações para a mesma
      # porta (127.0.0.1:8888 e 0.0.0.0:8888). A segunda colide com a
      # primeira e o MediaMTX nunca sobe — "address already in use" sobre uma
      # porta que, medida no host, estava livre.
      #
      # Usando as mesmas variáveis, base e overlay produzem entradas
      # IDÊNTICAS, que o Compose deduplica. O padrão continua sendo loopback.
      - "${DRAC_MEDIAMTX_HLS_BIND:-127.0.0.1}:8888:8888"   # HLS  → nginx /hls/
      - "${DRAC_MEDIAMTX_WEBRTC_HTTP_BIND:-127.0.0.1}:8889:8889"   # WebRTC WHEP → nginx /webrtc/
      # Mídia WebRTC (ICE/UDP): PRECISA ser pública — o browser conecta direto no IP anunciado
      # (mediamtx.yml webrtcAdditionalHosts) nesta porta. Sem ela o vídeo WebRTC não flui.
      - "${DRAC_MEDIAMTX_WEBRTC_UDP_BIND:-0.0.0.0}:8189:8189/udp"
      # INGESTÃO RTMP: a porta pública 1935 é atendida pelo serviço `rtmp-ingest`
      # (SRS, tradutor de dialeto), NÃO diretamente pelo MediaMTX. Medido em
      # campo: câmera legada Adobe/FMLE exige `onFCPublish`, que o MediaMTX não
      # envia — ver o comentário no serviço rtmp-ingest. O MediaMTX segue
      # escutando RTMP na rede interna (:1935 dentro do compose), que é por onde
      # o SRS repassa, e é lá que a publicação é autorizada contra o cadastro.
      # TCP na mesma porta: REMOVIDO a pedido do dono, restaurando o transporte
      # de 21/07 (só UDP), que rodava estável.
      #
      # Ele havia sido acrescentado como candidato ICE de reserva para rede que
      # bloqueia UDP — justificativa legítima. Mas WebRTC sobre TCP tem bloqueio
      # de fila e retransmissão: quando o ICE escolhe TCP, o vídeo chega em
      # rajadas e o operador vê fps oscilando e travada, que é o sintoma vivido
      # aqui. Medido antes de remover: as 18 sessões ativas estavam TODAS em UDP,
      # ou seja, nesta instalação o candidato TCP não entregava benefício algum
      # e só acrescentava um caminho ruim que o ICE podia escolher.
      #
      # Para reativar (cliente cuja rede realmente bloqueia UDP): republicar
      # "8189:8189/tcp" aqui E devolver `webrtcLocalTCPAddress: :8189` no
      # mediamtx.yml — os dois juntos, senão o candidato é anunciado e não
      # responde, que é pior que não existir.
    # init: roda o tini como PID 1, que COLHE processos zumbis. Sem isto o MediaMTX
    # (PID 1) não faz reaping e cada ffmpeg de restream órfão vira zumbi que acumula
    # para sempre (já observado: centenas). Em escala isso esgota a tabela de PIDs.
    init: true
    logging: *default-logging
    volumes:
      - ./mediamtx.yml:/mediamtx.yml:ro
    environment:
      - MEDIAMTX_API_USER=${MEDIAMTX_API_USER:?MEDIAMTX_API_USER is required}
      - MEDIAMTX_API_PASS=${MEDIAMTX_API_PASS:?MEDIAMTX_API_PASS is required}
      - MEDIAMTX_AUTH_CALLBACK_TOKEN=${MEDIAMTX_AUTH_CALLBACK_TOKEN:?MEDIAMTX_AUTH_CALLBACK_TOKEN is required}
      - MTX_AUTHHTTPADDRESS=http://api:3000/camera-stream/mediamtx-auth?internalToken=${MEDIAMTX_AUTH_CALLBACK_TOKEN:?MEDIAMTX_AUTH_CALLBACK_TOKEN is required}
      - MEDIAMTX_HLS_ALLOW_ORIGIN=${MEDIAMTX_HLS_ALLOW_ORIGIN:-*}
      - MEDIAMTX_WEBRTC_ALLOW_ORIGIN=${MEDIAMTX_WEBRTC_ALLOW_ORIGIN:-*}
      - MTX_HLSALLOWORIGINS=${MEDIAMTX_HLS_ALLOW_ORIGIN:-*}
      - MTX_WEBRTCALLOWORIGINS=${MEDIAMTX_WEBRTC_ALLOW_ORIGIN:-*}
      - MEDIAMTX_WEBRTC_ADDITIONAL_HOST=${MEDIAMTX_WEBRTC_ADDITIONAL_HOST:-127.0.0.1}
      # Override nativo do MediaMTX: aplica o host WebRTC (ICE) renderizado POR
      # INSTALAÇÃO (install-drac.sh → DRAC_SERVER_IP), no lugar do valor fixo do
      # yml. Sem esta linha o mediamtx.yml (hardcoded) vencia — instalação
      # white-label nova nascia anunciando o host ICE errado.
      - MTX_WEBRTCADDITIONALHOSTS=${MEDIAMTX_WEBRTC_ADDITIONAL_HOST:-127.0.0.1}
    healthcheck:
      test: ["CMD-SHELL", "nc -z 127.0.0.1 8554 || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 20s
    networks:
      - vms-net
      # rtmp-net: rede dedicada só entre o SRS e o MediaMTX, para o tradutor
      # RTMP alcançar mediamtx:1935 SEM enxergar postgres/redis/api.
      - rtmp-net

  # ── AVALIAÇÃO go2rtc (Fase 0 do plano híbrido, 2026-07-21) ──────────────────
  # Sidecar ISOLADO para comparação A/B com o MediaMTX. NÃO serve produção:
  # nenhuma câmera, o nginx e a API não apontam para cá. Portas em loopback e
  # fora das faixas do MediaMTX (8888/8889/8189) para não haver conflito.
  # Sob o profile 'eval': só sobe com `--profile eval`, nunca num `up` normal.
  go2rtc-eval:
    image: alexxit/go2rtc:latest
    container_name: vms-go2rtc-eval
    restart: unless-stopped
    profiles: ["eval"]
    init: true
    logging: *default-logging
    ports:
      - "127.0.0.1:1984:1984"     # API/UI de teste
      - "127.0.0.1:8555:8555/tcp" # WebRTC (mídia) — avaliação local
      - "8555:8555/udp"           # ICE/UDP p/ teste de latência real no browser
    volumes:
      - ./go2rtc.yml:/config/go2rtc.yaml:ro
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:1984/api || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 15s
    networks:
      - vms-net

volumes:
  postgres_data:

networks:
  vms-net:
    driver: bridge
  # Rede dedicada só entre o tradutor RTMP (SRS) e o MediaMTX. O SRS fica SÓ
  # aqui; o MediaMTX fica nas duas. Assim o parser RTMP em C++ exposto à
  # internet alcança apenas mediamtx:1935 — nunca postgres, redis ou api.
  rtmp-net:
    driver: bridge
